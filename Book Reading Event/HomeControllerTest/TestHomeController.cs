using Book_Reading.Controllers;
using Book_Reading.Models;
using Microsoft.AspNetCore.Mvc;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace HomeControllerTest
{
    [TestClass]
    public class TestHomeController
    {
        /*[TestMethod]
        public void TestDetails()
        {
            HomeController controller = new HomeController(null, null);
            CreateModel viewName = new CreateModel();
            string loc = viewName.Location;
            ViewResult result = controller.Details() as ViewResult;

            Assert.AreEqual(viewName, result.ViewName);
        }*/
    }
}
